{
  "name": "@freshcontext/server",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "dev": "tsx watch src/start.ts",
    "start": "node dist/start.js",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@freshcontext/hydra": "workspace:*",
    "fastify": "5.12.0"
  }
}
