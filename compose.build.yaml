services:
  freshcontext:
    image: freshcontext-local:build
    build:
      context: .
      dockerfile: Dockerfile
      target: runtime

  mcp:
    image: freshcontext-mcp-local:build
    build:
      context: .
      dockerfile: Dockerfile
      target: mcp-runtime
